# templates/ — View-Templates, werden ausschließlich vom Controller via
# require eingebunden. Kein direkter HTTP-Zugriff erlaubt.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
